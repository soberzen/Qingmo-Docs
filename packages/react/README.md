# React

React bindings for the Qingmo Docs editor core.

## Scripts

- `pnpm build`
- `pnpm dev`
- `pnpm clean`
- `pnpm check-types`

## Structure

- `src/index.ts` public entry
- `dist/` build output
