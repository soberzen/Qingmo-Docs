# Server

NestJS backend service for Qingmo Docs.

## Scripts

- `pnpm build`
- `pnpm start`
- `pnpm start:dev`
- `pnpm start:debug`
- `pnpm start:prod`

## Structure

- `src/` application source
- `dist/` build output
